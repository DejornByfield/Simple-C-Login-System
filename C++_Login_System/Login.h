#pragma once
#include <string>
using namespace std; 
class Login
{
public: 
	void login(string username, string password);
	void registration(string username, string password); 
	void welcome(); 
};

